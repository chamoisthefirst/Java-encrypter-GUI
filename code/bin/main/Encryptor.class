// Source code is decompiled from a .class file using FernFlower decompiler.
package main;

public class Encryptor {
   String key = "";

   public Encryptor() {
   }

   void changeKey(String newKey) {
      this.key = newKey;
   }

   String encrypt(String text) {
      String out = "";
      int count = 0;
      char[] var7;
      int var6 = (var7 = text.toCharArray()).length;

      for(int var5 = 0; var5 < var6; ++var5) {
         char thisChar = var7[var5];
         int num = thisChar + this.keyRead(this.key, count);
         ++count;
         out = out + (char)num;
      }

      return out;
   }

   String decrypt(String text) {
      String out = "";
      int count = 0;
      char[] var7;
      int var6 = (var7 = text.toCharArray()).length;

      for(int var5 = 0; var5 < var6; ++var5) {
         char thisChar = var7[var5];
         int num = thisChar - this.keyRead(this.key, count);
         ++count;
         out = out + (char)num;
      }

      return out;
   }

   int keyRead(String key, int location) {
      char[] charArray = key.toCharArray();
      int num = charArray[location % key.length()];
      location %= key.length();
      char[] keyCharArray = key.toCharArray();
      int out = keyCharArray[location];
      out += num;
      return out % 20;
   }
}
